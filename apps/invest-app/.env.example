# Credenciais do Supabase (backend do produto Investimentos).
# Copie este arquivo para ".env" e preencha com os dados do SEU projeto Supabase.
# Sem essas variáveis, o app roda em "modo local" (sem login, dados só no navegador).

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
